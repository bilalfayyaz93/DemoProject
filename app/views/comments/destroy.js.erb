$('.delete').bind('ajax:success',function()
{
  $(this).closest('div').parent().parent().fadeOut();
}
);
var text = $('#comments-count').text();
var ind = text.indexOf('(') + 1;
var count = parseInt(text.substr(ind))-1;
$('#comments-count').html('Comments ( '+count + ' )');
